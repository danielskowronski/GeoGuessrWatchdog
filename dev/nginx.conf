server {
  listen 80;

  location / {
    root /srv/ggwd_cache;
    autoindex on;
    autoindex_exact_size off;
    autoindex_localtime on;

    default_type application/json;
    types {
      application/json json;
    }
  }
}